// Separation strip retired for public launch. Kept as an inert export so
// existing imports continue to compile without rendering any strip.
export function LegalSeparationStrip(_props: { compact?: boolean }) {
  return null;
}
