// Notice retired for public launch. Kept as an inert export so existing
// imports continue to compile without rendering any banner.
export function LegalStatusNotice(_props: { variant?: "banner" | "inline" }) {
  return null;
}
